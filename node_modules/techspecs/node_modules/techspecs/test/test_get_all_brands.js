import { techspecs } from "techspecs";

(async () => {
  const techspecs_api_key = "techspecs_api_key";
  const techspecs_base_url = "https://api.techspecs.io";
  const response = await techspecs.get_all_brands(
    techspecs_base_url,
    techspecs_api_key,
    (mode = "pretty")
  );

  console.log(response);
})();
